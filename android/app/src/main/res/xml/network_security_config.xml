<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain-HTTP ("cleartext") traffic by default since API 28.
  The dev backend runs over plain HTTP, so this scopes an exception to just
  the local-dev hosts instead of disabling the protection app-wide.

  10.0.2.2:       the Android emulator's alias for the host machine
  localhost/127.: for a device connected via `adb reverse`

  For a real deployment, point VITE_API_URL (see .env.mobile) at an HTTPS
  API domain and remove this file (or use a cleartext-permitted allowlist
  containing only that domain, if it must stay HTTP).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
